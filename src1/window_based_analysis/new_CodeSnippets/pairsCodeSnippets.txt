pairs(pca, 1:5], col = mycolors)
pairs(iris, col=cols)
pairs(pc.cr, col=c)
pairs(rbind, col = cl, lower.panel=low.panelfun, upper.panel=up.panelfun)
pairs(k)
pairs(f, col=1:ncol, upper.panel=NULL, main="Factor loadings")
pairs(daq)
pairs(tmp, lower.panel=panel.cor, upper.panel=panel.my.points, labels=c)
pairs(f$loadings, factors=5)
pairs(iris, upper.panel = panel.cor)
pairs(X, xlim=c, ylim=c)
pairs(x.mat, col = cl, lower.panel=low.panelfun, upper.panel=up.panelfun)
pairs(Dat, log="xy")
pairs(dat[, -5], lower.panel = foo.lower, upper.panel = foo.upper, data)
pairs(USJudgeRatings[)
pairs(pc$x, c)
pairs(dat, labels=c)
pairs(banknote[)
pairs(as.matrix, lower.panel = panel.low, upper.panel = panel.up)
pairs(U, panel = mypanel, diag.panel=mydiag.panel, label.pos = 0.5)
pairs(e1, e2)
pairs()
pairs(list, panel = function)
pairs(m, panel=panelfun, panel=panelfun, pch=16)
pairs(iris[1:4], main = "Anderson's Iris Data -- 3 species", pch = 21, bg = c, oma=c)
pairs(data.matrix)
pairs(iris[, 2:4])
pairs(as.data.frame, upper.panel = pnl, diag.panel = pnl, cex.labels = 1)
pairs(m, col=cols)
pairs(dat[, -5], lower.panel = foo.lower, upper.panel = foo.upper, ind.upper = dat, ind.lower = dat, col.upper = 'blue', col.lower = 'red')
pairs(as.matrix, vars], lower.panel = panel.low, upper.panel = panel.up)
pairs(USJudgeRatings, lower.panel = my_line, upper.panel = panel.cor)
pairs(iris)
pairs(ddbind[, grep)
pairs(C, col=c)
pairs(a[, 2:3], col=a[, 1])
pairs(data)
pairs(iris[, 1:4], lower.panel=panel.Kendall, upper.panel=panel.points)
pairs(iris[1:4], main = "Width", pch = 24, bg = c, upper.panel=NULL, labels=c, font.labels=1, cex.labels=2)
pairs(data[i)
pairs(my_rasters)
pairs(testData)
